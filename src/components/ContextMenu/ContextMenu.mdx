---
name: ContextMenu
route: /components/context-menu
menu: Components
---

# ContextMenu
Create a beatiful context menu

## Code Example
```jsx
import React from "react";

import EditIcon from "@material-ui/icons/EditOutlined"
import DeleteIcon from "@material-ui/icons/DeleteOutlined"

import { ContextMenu, ContextMenuItem } from "components/ContextMenu";

export const MyComponent: React.FC = () => {
  return (
    <ContextMenu>
      <ContextMenuItem title="Edit" icon={EditIcon} onClick={() => alert("edit")} />
      <ContextMenuItem fixed={false} title="Delete" icon={DeleteIcon} onClick={() => alert("Delete")} />
    </ContextMenu>
  );
}

```

## Basic Usage
import { Props } from "docz";

import { ThemeProvider, List, ListItem, ListItemText, ListItemSecondaryAction } from "@material-ui/core";
import { createMuiTheme } from "@material-ui/core/styles";
import EditIcon from "@material-ui/icons/EditOutlined";
import DeleteIcon from "@material-ui/icons/DeleteOutlined";

import { ContextMenu } from "./ContextMenu";
import { ContextMenuItem } from "./ContextMenuItem";

### With fixed property
<Playground>
  <ThemeProvider theme={createMuiTheme()}>
    <List>
      {
        ['max', 'john', 'richard'].map(user => (
          <ListItem key={user}>
            <ListItemText primary={user} />
            <ListItemSecondaryAction>
              <ContextMenu>
                <ContextMenuItem fixed={false} title="Edit" icon={EditIcon} onClick={() => alert("edit")} />
                <ContextMenuItem title="Delete" icon={DeleteIcon} onClick={() => alert("Delete")} />
              </ContextMenu>
            </ListItemSecondaryAction>
          </ListItem>
        ))
      }
    </List>
  </ThemeProvider>
</Playground>

---
### Without fixed property
<Playground>
  <ThemeProvider theme={createMuiTheme()}>
    <Typography></Typography>
    <List>
      {
        ['max', 'john', 'richard'].map(user => (
          <ListItem key={user}>
            <ListItemText primary={user} />
            <ListItemSecondaryAction>
              <ContextMenu>
                <ContextMenuItem title="Edit" icon={EditIcon} onClick={() => alert("edit")} />
                <ContextMenuItem title="Delete" icon={DeleteIcon} onClick={() => alert("Delete")} />
              </ContextMenu>
            </ListItemSecondaryAction>
          </ListItem>
        ))
      }
    </List>
  </ThemeProvider>
</Playground>

## Props

### ContextMenu
This component don't have properties

### ContextMenuItem
<Props of={ContextMenuItem} />
